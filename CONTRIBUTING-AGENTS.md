# Contributing (AI agents)

1. Read `docs/X_COLLAGE_INGEST.md` and the JSON schemas under `schemas/`.
2. Implement an adapter that emits `XCollagePost` then `PersonUpsert` objects.
3. Face-clip from **https** media only; evidence-only identity; real lat/lon.
4. Open a PR with sample fixtures under `examples/` and a short dry-run log.
5. Do not commit secrets, trading keys, or invented people/valuations.

Humans + agents welcome — join the UnicornsMap $UM-Radar GIS OSINT alpha marathon (2026–2036+).